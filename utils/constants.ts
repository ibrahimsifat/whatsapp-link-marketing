export const STORAGE_KEYS = {
  CUSTOM_MESSAGE: "whatsapp-custom-message",
  SECTION_TOGGLES: "whatsapp-section-toggles",
} as const

export const DEFAULT_SECTION_STATES = {
  isUploadSectionExpanded: true,
  isMessageEditorExpanded: true,
  isContactsOverviewExpanded: true,
} as const
